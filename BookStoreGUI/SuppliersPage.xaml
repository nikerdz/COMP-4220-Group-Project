<UserControl x:Class="BookStoreGUI.Pages.SuppliersPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Manage Suppliers"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0 0 0 10"/>

        <DataGrid x:Name="SuppliersGrid"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  Margin="0 10 0 10">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Supplier ID" Binding="{Binding SupplierId}" Width="120"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
            </DataGrid.Columns>

        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Add" Width="80" Margin="5" Click="BtnAdd_Click"/>
            <Button Content="Edit" Width="80" Margin="5" Click="BtnEdit_Click"/>
            <Button Content="Delete" Width="80" Margin="5" Click="BtnDelete_Click"/>
        </StackPanel>

    </Grid>
</UserControl>
